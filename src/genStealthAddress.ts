export function generateStealthAddress({
  spendingPublicKey, // P_spend
  derivedPrivateKey, // p_derived
}: {
  spendingPublicKey: `0x${string}`;
  derivedPrivateKey: `0x${string}`;
}): { stealthAddress: `0x${string}` } {
  // P_stealth_address
  // TODO: implement this
  return null as unknown as { stealthAddress: `0x${string}` };
}
